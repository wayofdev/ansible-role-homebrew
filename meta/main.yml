---

galaxy_info:
  role_name: homebrew
  author: wayofdev
  description: Ansible role that uses loops and retries to install Homebrew apps and casks.
  company: wayofdev

  license: GPL-3.0-only

  min_ansible_version: "2.12"

  platforms:
    - name: macOS
      versions:
        - "Big-Sur"

  galaxy_tags:
    - osx
    - mac
    - brew
    - homebrew

dependencies: []

# Ensure that custom Ansible plugins and modules included in the main MacOps
# collection are available to roles in other collections.
collections: ["wayofdev.macops"]
