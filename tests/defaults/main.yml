---

brew:
  repository: https://github.com/Homebrew/brew
  branch: master

  # Run task to upgrade all packages
  upgrade_all: false

  taps:
    - homebrew/core
    - denji/nginx
    - homebrew/cask-fonts

  # Homebrew Packages, Installed to the default homebrew location.
  # (Usually /usr/local, but depends on your homebrew install configuration.)
  packages:
    - ssh-copy-id  # from homebrew/core
    - nginx-full  # from dengi/nginx

  # Standard /Applications Installed Brew Casks
  casks:
    - firefox  # from hombrew/cask
    - google-chrome  # from hombrew/cask

  # How much times to retry, if installation of package / tap / cask fails ? This can happen during network problems.
  retries: 64

  # Delay between each retry attempt.
  delay: 3

  # Clear homebrew cache after installing packages / casks
  clear_cache: false

  cask_appdir: /Applications

  # Allow external apps.
  # Default brew value: false
  accept_external_casks: true

  # Upgrade casks that auto update.
  # Passes –greedy to brew cask outdated when checking if an installed cask has a newer version available.
  # Default: false
  cask_greedy_mode: false
